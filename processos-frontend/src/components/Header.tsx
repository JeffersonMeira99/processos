import MenuLateral from './Drawer/Drawer';

export const Header = () => {
    /*     const logout = useAuthStore(state => state.logoutUser);
     */
    return (
        <>
            <header>
                <MenuLateral />
            </header>
        </>
    );
};
